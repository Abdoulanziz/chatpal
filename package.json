{
  "name": "chatapp",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "yarn install --prefix backend && yarn install --prefix frontend && yarn build --prefix frontend",
    "start": "yarn run start --prefix backend"
  },
  "license": "MIT"
}
