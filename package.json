{
  "name": "chatapp",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "yarn install --frozen-lockfile && cd frontend && yarn build && cd ../backend && yarn install",
    "start": "yarn run start --prefix backend"
  },
  "license": "MIT"
}
